# robots.txt for LeadCompany

User-agent: *
Disallow: /private/
Allow: /

# This configuration allows all user agents (web crawlers) to access the site
# while disallowing access to the /private/ directory.
# Adjust the paths as necessary based on your project's structure